# Database Configuration
# For local development, leave POSTGRES_URL unset to use SQLite fallback
# For production deployment on Vercel, set POSTGRES_URL to your Postgres connection string

# POSTGRES_URL=postgres://username:password@hostname:port/database?sslmode=require
POSTGRES_URL=""

# Optional: Custom SQLite database path (defaults to timetracker.db)
# SQLITE_DB_PATH=./timetracker.db

# App environment
NEXT_PUBLIC_APP_NAME="Hours Tracker"
